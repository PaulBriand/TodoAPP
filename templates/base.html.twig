<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">

		<title>
			{% block title %}{% endblock %}
		</title>

		<link rel="stylesheet" href="https://bootswatch.com/5/cerulean/bootstrap.min.css">
		{% block stylesheets %}
			{#{{ encore_entry_link_tags('app') }}#}
		{% endblock %}

		{% block javascripts %}
			{#{{ encore_entry_script_tags('app') }}#}
		{% endblock %}
	</head>

	<body class="text-center row">
		<div class="text-start mt-2 col-4">
			<button>
				<a href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge({ '_locale': 'en' })) }} "><img src="{{ asset('assets/images/flag_angleterre.png') }}" alt=" english"></a>
			</button>
			<button>
				<a href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge({ '_locale': 'fr' })) }} "><img src="{{ asset('assets/images/flag_france.png') }}" alt="Francais"></a>
			</button>
		</div>

		<<<<<<<HEAD<bodyclass="text-center"><aclass="btnbtn-danger"href="{{path("app_logout")}}">{{'general.button.logout'|trans}}</a><p>{{'general.login'|trans}}:{{app.user.email}}</p>=======<divclass="text-centermt-2col-4"><p>{{'general.login'|trans}}:<br>{{app.user.email}}</p></div><divclass="text-endmt-2col-4"><aclass="btnbtn-danger"href="{{path("app_logout")}}">Logout</a></div>>> {% if is_granted('IS_AUTHENTICATED_FULLY') %} {% if is_granted('IS_AUTHENTICATED_FULLY') %} {% endif %} {% if is_granted('IS_AUTHENTICATED_FULLY') %} {% endif %}>>>>> 2cbc7c011c4146ddf87ce70a2a3dfa018bcf3acb

		{% block body %}{% endblock %}
	</body>

	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</html>
