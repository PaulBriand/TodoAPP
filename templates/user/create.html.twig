{% extends "base.html.twig" %}

{% block title %}

	{{'general.title.addUser'|trans}}

{% endblock %}

{% block body %}

	<br><br>
	<div class="d-flex justify-content-center">
		<h1>{{'general.title.addUser'|trans}}</h1>
	</div>
	<br><br>
	<div class="container">
		{{ form_start(form) }}
		{{ form_widget(form) }}
		{{ form_end(form) }}
		<div>
			<a href="/fr/user/listing" class="btn btn-primary btn-lg active" role="button" aria-pressed="true">{{'general.button.back'|trans}}</a>
			<a href="/fr/task/listing" class="btn btn-warning btn-lg active" role="button" aria-pressed="true">{{'general.button.accueil'|trans}}</a>
		</div>

	</div>

{% endblock %}
